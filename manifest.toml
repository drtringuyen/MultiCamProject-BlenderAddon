schema_version = "1.0.0"

id = "multicamproject"
version = "0.0.1"
name = "MultiCamProject"
tagline = "Project camera photos onto scans, bake them and export to Unity"
maintainer = "drtringuyen"
type = "add-on"

blender_version_min = "5.2.0"

license = ["SPDX:GPL-3.0-or-later"]

tags = ["Material", "Bake", "Import-Export"]

[permissions]
files = "Save baked textures, FBX files and the Unity import script"

# build_extension.py writes this file into the zip as blender_manifest.toml.
# --full adds: wheels = [...] and platforms = ["windows-x64"]
