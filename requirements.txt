# Add your Python dependencies here
# Example:
# numpy>=1.20.0
# pillow>=8.0.0

# Note: Blender includes most common packages (PIL, numpy, etc.)
# Only add dependencies if they're not included in Blender
